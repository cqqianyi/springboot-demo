server:
  port: 8080
spring:
  cache:
    type: redis
  redis:
     host: 127.0.0.1 # server host
     port: 6379 # connection port
     pool.max-idle: 8 # pool settings ...
     pool.min-idle: 1
     pool.max-active: 8
     pool.max-wait: -1
     database: 0
